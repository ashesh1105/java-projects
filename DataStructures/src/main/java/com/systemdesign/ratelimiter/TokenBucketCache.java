package com.systemdesign.ratelimiter;

// Singleton class. We could also create an enum since enum constants are always singleton
public class TokenBucketCache implements RulesCache {

    // Any other implementation you need it, add it here. get and post on RulesCache is already defined
    // as static methods in the interface
}
